version: 1
applications:
    - frontend:
          phases:
              preBuild:
                  commands:
                      - node --version
                      - npm install
              build:
                  commands:
                      - npm run build
          artifacts:
              baseDirectory: dist
              files:
                  - "**/*"
          cache:
              paths:
                  - node_modules/**/*
      appRoot: sifu
